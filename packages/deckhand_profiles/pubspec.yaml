name: deckhand_profiles
description: Fetch and parse Deckhand printer profiles from deckhand-profiles.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  deckhand_core:
    path: ../deckhand_core
  deckhand_flash:
    path: ../deckhand_flash
  dio: ^5.9.1
  yaml: ^3.1.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  deckhand_lints:
    path: ../deckhand_lints
