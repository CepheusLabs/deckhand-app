name: deckhand_discovery
description: LAN discovery (mDNS + CIDR scan + SSH-ready polling).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  deckhand_core:
    path: ../deckhand_core
  bonsoir: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  deckhand_lints:
    path: ../deckhand_lints
