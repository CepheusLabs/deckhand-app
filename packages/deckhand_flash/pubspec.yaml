name: deckhand_flash
description: Disk enumeration + image flashing via the Go sidecar.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  deckhand_core:
    path: ../deckhand_core
  crypto: ^3.0.5
  uuid: ^4.0.0
  flutter_secure_storage: ^10.0.0
  dio: ^5.9.1
  meta: ^1.15.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  deckhand_lints:
    path: ../deckhand_lints
